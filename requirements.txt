# ---- Core pipeline (always required) ----
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
joblib>=1.3
matplotlib>=3.7

# ---- Pipeline (optional, for Phase 1 geo loaders) ----
pyarrow>=14.0
geopandas>=0.14
shapely>=2.0
fiona>=1.9
rasterio>=1.3

# ---- Pipeline (optional, for upgrades) ----
xgboost>=2.0
statsmodels>=0.14
prophet>=1.1
openmeteo-requests>=1.3   # only if re-fetching weather from the API
requests-cache>=1.1
retry-requests>=2.0
osmium>=3.6               # only if re-extracting roads from PBF

# ---- Web app (Phase 7) ----
fastapi>=0.110
uvicorn[standard]>=0.25
pydantic>=2.5
